extends ../layouts/application.pug

block title
  | Log In — The Debt Collective

block content
  .wrapper.py4
    include ../includes/sessions/new-form.pug

block scripts
  script(src="/build/javascripts/sessions/new.js" async)
  script.
    window.addEventListener('load', function() {
      var options = {
        currentUser: !{JSON.stringify(currentUser)},
        currentURL: !{JSON.stringify(currentURL)},
      };
      try {
        new ViewSessionsNew(options);
      } catch(e) {
        airbreak.notify(e);
      }
    }, true);
