const authenticate = require('../../../services/authentication');

describe('authentication', () => {
  const noop = () => {};

  describe('by cookie', () => {
    xit('should set the user using the cookie', () => {});
  });
});
