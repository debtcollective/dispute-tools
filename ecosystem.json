{
  /**
   * Application configuration section
   * http://pm2.keymetrics.io/docs/usage/application-declaration/
   */
  apps : [
    {
      name: 'TDC',
      script: 'bin/server.js',
      watch: true,
      instances: 2,
      exec_mode: 'fork',
      env_staging: {
        NODE_ENV: 'staging'
      }
    },
  ],

  deploy: {
    staging : {
      user : "deploy",
      host : "54.162.119.96",
      ref  : "origin/master",
      repo : "git@gitlab.com:empathia/debtcollective.git",
      path : "/deploy/apps/tdc",
      "post-deploy" : "npm install; knex migrate:latest; npm run build; pm2 startOrRestart ecosystem.json --env staging"
    },
  }
}
